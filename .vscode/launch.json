{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Node.JS App",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}\\dist\\app.js",
            "args": [],
            "preLaunchTask": "npm: build"
        }
    ]
}
